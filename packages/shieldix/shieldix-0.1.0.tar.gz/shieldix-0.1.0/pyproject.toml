[build-system]
requires = ["setuptools>=69.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "shieldix"
version = "0.1.0"
description = "A standalone Python library with no external dependencies"
authors = [{name = "MERO"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.6"
keywords = ["shield", "security", "protection", "standalone"]

dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.4.3",
    "black>=23.12.0",
    "flake8>=6.1.0"
]

[project.urls]
Homepage = "https://t.me/QP4RM"
Issues = "https://t.me/QP4RM"

[tool.setuptools]
packages = {find = {}}

[tool.setuptools.package-data]
shieldix = []
