[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mypackage-prashant-Chouhan"   # must be unique on PyPI
version = "0.1.0"
description = "A tiny example package (PoC)"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{name="Prashant Chouhan", email="prashantchouhan771@gmail.com"}]
dependencies = []
