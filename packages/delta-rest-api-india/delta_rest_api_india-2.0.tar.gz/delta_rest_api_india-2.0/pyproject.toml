[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "delta-rest-api-india"
version = "2.0"
description = "DELTA ALOG "
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
    { name="Shashank Joshi", email="shashankjoshi61@yahoo.com" }
]
keywords = ["delta", "delta api", "crypto algo", "delta algo"]

dependencies = [
    "requests",
    "ta",
    "ccxt",
    "pandas"
]


