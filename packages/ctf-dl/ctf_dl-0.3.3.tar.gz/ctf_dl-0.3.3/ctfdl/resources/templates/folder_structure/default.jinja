{# description: Organizes by category and challenge name #}
{{ challenge.category | slugify }}/{% if challenge.subcategory %}{{ challenge.subcategory | slugify }}/{% endif %}{{ challenge.name | slugify }}