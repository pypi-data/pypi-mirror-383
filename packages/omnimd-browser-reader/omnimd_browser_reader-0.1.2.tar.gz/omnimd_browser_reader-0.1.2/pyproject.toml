[project]
name = "omnimd-browser-reader"
version = "0.1.2"
description = "A plugin for omni-article-markdown to read content that needs enabling javascript."
authors = [
    { name = "Lei", email = "caol64@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.13"
license = "MIT"
dependencies = [
    "playwright",
]

[project.entry-points.mdcli]
browser = "browser"

[project.urls]
Homepage = "https://github.com/caol64/omni-article-markdown/tree/main/plugins/omnimd-browser-reader"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
exclude = [
    "/dist",
]

[tool.hatch.build.targets.wheel]
include = [
    "/browser.py",
]

[tool.hatch.build.targets.sdist]
include = [
    "/browser.py",
    "/README.md",
    "/pyproject.toml",
]
