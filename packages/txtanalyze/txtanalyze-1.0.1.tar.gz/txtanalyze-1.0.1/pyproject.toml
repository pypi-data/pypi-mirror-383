[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "txtanalyze"
version = "1.0.1"
authors = [
  {name="Dimuth Sakya", email="dimuthsakya@protonmail.com"}
]
description = "A modular Python package for analyzing text paragraphs"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
