[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "rypi"
version = "2025.10.13.1.0"
description = "Rybby's Python tools."
readme = "README.md"
requires-python = ">=3.8"
urls = { Homepage = "https://rymaa.cn" }
authors = [
    {name = "rybby", email = "rybby@163.com"}
]
license = { text = "MIT" }
classifiers = [
    "Environment :: WebAssembly :: WASI"
]
dependencies = [
    # 第三方依赖
]

[project.optional-dependencies]
test = []
dev = []

[project.scripts]
rypi = "rypi.main:main"