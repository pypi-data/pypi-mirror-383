# API base URL (not secret, can be changed for dev/staging/prod)
AIRBELD_API_BASE=https://api.airbeld.com

# API token (JWT). Leave empty here.
# Developers should obtain a token from /api/v1/auth/token/
# and export it locally, never commit real tokens to git.
AIRBELD_API_TOKEN=REPLACE_WITH_REAL_JWT

# Authentication (for standalone examples only)
# Do not use these credentials with Home Assistant integration
AIRBELD_USER_EMAIL=
AIRBELD_USER_PASSWORD=