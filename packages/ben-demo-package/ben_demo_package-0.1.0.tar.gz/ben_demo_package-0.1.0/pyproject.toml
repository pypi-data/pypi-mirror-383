[project]
name = "ben-demo-package"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ben", email = "bennyp2494@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "scipy>=1.13.1",
]

[project.scripts]
ben-demo-package = "ben_demo_package:main"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
