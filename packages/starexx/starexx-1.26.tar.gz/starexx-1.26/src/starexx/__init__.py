from .core import (
    say, ask, shout, whisper, echo, askNumber, askChoice,
    add, subtract, multiply, divide, square, cube, power, sqrt,
    average, maximum, minimum, total, percentage, isEven, isOdd,
    wait, showTime, showDate, countdown, repeat, ifTrue, ifFalse, choose,
    splitText, joinText, reverseText, textLength, makeUnique, sortItems,
    findInList, countInList, writeFile, readFile, appendFile, deleteFile,
    fileExists, listFiles, clearScreen, pause, exitApp, openWebsite,
    searchWeb, saveData, loadData, roundNumber, floorNumber, ceilingNumber,
    absoluteValue, isPrime, factorial, fibonacci, createList, createDict,
    getKeys, getValues, addToDict, removeFromDict, mergeDicts, toUpper,
    toLower, capitalizeText, titleText, replaceText, trimText, startsWith,
    endsWith, findText, countText, convertCtoF, convertFtoC, convertKMtoMiles,
    convertMilesToKM, getCurrentFolder, changeFolder, createFolder, isFile,
    isFolder, fileSize, validateEmail, validateNumber, formatCurrency,
    formatPercent, formatNumber, getSystemInfo, timerStart, timerStop,
    measureTime, createCounter, sayHello, sayGoodbye, calculateArea,
    calculatePerimeter
)

__version__ = "1.26"
__author__ = "Ankit Mehta"
