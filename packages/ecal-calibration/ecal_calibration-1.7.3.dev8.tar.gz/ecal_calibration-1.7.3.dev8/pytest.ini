[pytest]
testpaths        = tests
addopts          = -v --disable-warnings -x
python_files     = test_*.py
python_classes   = Test*
python_functions = test_*
log_level        = DEBUG
timeout          = 30
markers          =
    slow: marks tests as slow (use with '-m slow')
    fast: marks tests as fast
