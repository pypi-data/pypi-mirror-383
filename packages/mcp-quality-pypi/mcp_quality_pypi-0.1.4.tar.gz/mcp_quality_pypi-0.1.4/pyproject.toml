[project]
name = "mcp-quality-pypi"
version = "0.1.4"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yanggy", email = "863007116@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.16.0",
]

[project.scripts]
mcp-quality-pypi = "mcp_quality_pypi:main"

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"
