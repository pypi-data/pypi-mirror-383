src = market_data_source(timeframe="1D")
# Create a boolean comparison
is_high = src.c > 100
# Use boolean as numeric input (should auto-cast to 1/0)
result = src.c + is_high
trade_signal_executor()(enter_long=(result > 101))