src = market_data_source()
rsi_val = rsi(period=14)(src.c)
oversold = rsi_val < 30

# Test None in ternary expression
filtered = oversold if oversold else None

# Test None in variable assignment
null_value = None
