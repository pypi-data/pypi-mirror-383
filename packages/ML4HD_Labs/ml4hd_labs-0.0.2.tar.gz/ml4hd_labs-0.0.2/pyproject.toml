[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ML4HD_Labs"
version = "0.0.2"
description = "Helper functions and utilities for ML4HD labs"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    # Add dependencies here, e.g., "requests"
    "numpy>=1.24.4",
    "opencv-python>=4.11.0.86",
]   
