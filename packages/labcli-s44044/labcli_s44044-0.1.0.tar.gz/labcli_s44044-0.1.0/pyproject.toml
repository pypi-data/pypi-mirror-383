[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "labcli-s44044"
version = "0.1.0"
description = "A simple CLI to list and run lab programs"
readme = "README.md"
license = "MIT"
authors = [
  { name = "Shakthivel C", email = "shakthic007@gmail.com" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
lab = "labcli.cli:main"

[tool.setuptools.packages.find]
where = ["labcli"]
