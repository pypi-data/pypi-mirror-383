[project]
name = "acex-cli"
version = "0.1.0"
description = "CLI components for acex framework"
authors = [
    {name = "Johan Lahti", email = "johan.lahti@acebit.se"}
]
requires-python = ">=3.13"
dependencies = [
    "acex-core"
]

[tool.poetry]
packages = [
    { include = "commands", to = "acex/cli" },
]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
