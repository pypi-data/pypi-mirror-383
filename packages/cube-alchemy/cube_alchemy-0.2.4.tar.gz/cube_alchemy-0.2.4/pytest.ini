[pytest]
testpaths = test
markers =
    unit: fast, isolated unit tests
    integration: tests that exercise multiple components and I/O
    slow: data-heavy or long-running tests
addopts = -ra
