[project]
name = "hx-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "2024103861", email = "2024103861@ruc.edu.cn" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.17.0",
]

[project.scripts]
hx-mcp-demo = "hx_mcp_demo:main"

[build-system]
requires = ["uv_build>=0.8.3,<0.9.0"]
build-backend = "uv_build"
