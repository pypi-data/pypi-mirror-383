apiVersion: v1
kind: ConfigMap
metadata:
  name: stac-server-config
  labels:
    app: stac-server
data:
  config.yaml: |
    {{- with .Values.stacServer.config }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
