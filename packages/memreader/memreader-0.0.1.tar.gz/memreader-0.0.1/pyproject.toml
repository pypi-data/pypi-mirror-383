[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "memreader"
version = "0.0.1"
description = "Read memory from dialogues."
readme = "README.md"
requires-python = ">=3.8"
