\providecommand\myfontsize{12pt}
$if(font_size)$
\renewcommand\myfontsize{$font_size$}
$endif$
$if(fontsize)$
\renewcommand\myfontsize{$fontsize$}
$endif$

$if(no_signature)$
$else$
\providecommand\includesignature{}
$endif$

$if(signaturefile)$\providecommand\signaturefile{$signaturefile$}$endif$

$if(border)$
\providecommand\myborder{$border$}
$else$
\providecommand\myborder{1in}
$endif$

$if(mainfont)$
\providecommand\myfont{$mainfont$}
$endif$

$if(colorlinks)$
\providecommand\mycolorlinks{$colorlinks$}
$endif$

$if(sansfont)$
\providecommand\mysansfont{$sansfont$}
$endif$

$if(spacing)$
\providecommand\myspacing{$spacing$}
$endif$

$if(parskip)$
\providecommand\myparskip{$parskip$}
$endif$

$if(parindent)$
\providecommand\myparindent{$parindent$}
$endif$

\documentclass[\myfontsize]{extarticle}
\usepackage{pandoc}
\usepackage{letterhead}

\begin{document}

$body$

\end{document}
