{
  "name": "module-name",
  "version": "1.0.0",
  "type": "no-ui",
  "computeType": "docker",
  "description": "Backend-only module for data processing",
  "entrypoint": "backend.main:NoUIModule",
  "author": {
    "name": "Developer Name",
    "email": "developer@example.com",
    "organization": "Organization"
  },
  "runtime": "python3.11",
  "inputs": {
    "data_source": {
      "type": "array",
      "description": "Data source (provide inline array; adjust manifest if using file or S3 URI)",
      "required": true
    },
    "options": {
      "type": "object",
      "description": "Processing options",
      "required": false,
      "properties": {
        "batch_size": {
          "type": "integer",
          "description": "Batch size for processing",
          "default": 100
        },
        "save_results": {
          "type": "boolean",
          "description": "Save results to storage",
          "default": false
        }
      }
    }
  },
  "outputs": {
    "results": {
      "type": "object",
      "description": "Processing results with metadata"
    }
  },
  "resources": {
    "memory": 512,
    "timeout": 300,
    "environment": {
      "LOG_LEVEL": "INFO"
    }
  },
  "permissions": {
    "database": ["read"],
    "storage": true
  },
  "dependencies": {
    "peptides": true,
    "storage": true,
    "api": true
  }
}
