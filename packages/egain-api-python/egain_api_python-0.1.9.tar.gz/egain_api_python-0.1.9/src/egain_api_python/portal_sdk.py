"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from .sdkconfiguration import SDKConfiguration
from egain_api_python.articlelists import Articlelists
from egain_api_python.connectorssearchevents import Connectorssearchevents
from egain_api_python.escalation import Escalation
from egain_api_python.export import Export
from egain_api_python.federatedsearchevent import Federatedsearchevent
from egain_api_python.general import General
from egain_api_python.guidedhelp import Guidedhelp
from egain_api_python.populararticles import Populararticles
from egain_api_python.portal_article import PortalArticle
from egain_api_python.portal_attachment import PortalAttachment
from egain_api_python.portal_bookmark import PortalBookmark
from egain_api_python.portal_suggestion import PortalSuggestion
from egain_api_python.portal_topic import PortalTopic
from egain_api_python.portal_userdetails import PortalUserdetails
from egain_api_python.portal_userprofile import PortalUserprofile
from egain_api_python.search import Search
from egain_api_python.usermilestones import Usermilestones
from typing import Optional


class PortalSDK(BaseSDK):
    article: PortalArticle
    topic: PortalTopic
    populararticles: Populararticles
    guidedhelp: Guidedhelp
    bookmark: PortalBookmark
    userprofile: PortalUserprofile
    userdetails: PortalUserdetails
    articlelists: Articlelists
    general: General
    search: Search
    suggestion: PortalSuggestion
    usermilestones: Usermilestones
    federatedsearchevent: Federatedsearchevent
    escalation: Escalation
    connectorssearchevents: Connectorssearchevents
    attachment: PortalAttachment
    export: Export

    def __init__(
        self, sdk_config: SDKConfiguration, parent_ref: Optional[object] = None
    ) -> None:
        BaseSDK.__init__(self, sdk_config, parent_ref=parent_ref)
        self.sdk_configuration = sdk_config
        self._init_sdks()

    def _init_sdks(self):
        self.article = PortalArticle(self.sdk_configuration, parent_ref=self.parent_ref)
        self.topic = PortalTopic(self.sdk_configuration, parent_ref=self.parent_ref)
        self.populararticles = Populararticles(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.guidedhelp = Guidedhelp(self.sdk_configuration, parent_ref=self.parent_ref)
        self.bookmark = PortalBookmark(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.userprofile = PortalUserprofile(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.userdetails = PortalUserdetails(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.articlelists = Articlelists(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.general = General(self.sdk_configuration, parent_ref=self.parent_ref)
        self.search = Search(self.sdk_configuration, parent_ref=self.parent_ref)
        self.suggestion = PortalSuggestion(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.usermilestones = Usermilestones(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.federatedsearchevent = Federatedsearchevent(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.escalation = Escalation(self.sdk_configuration, parent_ref=self.parent_ref)
        self.connectorssearchevents = Connectorssearchevents(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.attachment = PortalAttachment(
            self.sdk_configuration, parent_ref=self.parent_ref
        )
        self.export = Export(self.sdk_configuration, parent_ref=self.parent_ref)
