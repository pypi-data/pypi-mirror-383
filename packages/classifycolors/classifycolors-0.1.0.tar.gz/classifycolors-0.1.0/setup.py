from setuptools import setup, find_packages

setup(
    name="classifycolors",
    version="0.1.0",
    author="Elie Fares",
    packages=find_packages(),
    install_requires=[
        "webcolors"
    ]
)