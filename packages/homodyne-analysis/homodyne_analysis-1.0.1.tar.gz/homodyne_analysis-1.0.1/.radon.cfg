# Radon Configuration
# Prevents radon from trying to parse pyproject.toml with ConfigParser
# which causes interpolation syntax errors with pytest log format strings

[radon]
exclude = pyproject.toml
