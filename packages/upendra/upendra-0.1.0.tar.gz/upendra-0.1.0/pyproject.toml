[project]
name = "upendra"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Upendra Panda", email = "upendra.panda@intel.com" }
]
requires-python = ">=3.13.5"
dependencies = [
    "panda>=0.3.1",
    "rich>=14.2.0",
    "transformers>=4.57.0",
    "typer>=0.19.2",
]

[project.scripts]
upendra = "upendra.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = ["black>=24.0.0", "ruff>=0.6.0"]


