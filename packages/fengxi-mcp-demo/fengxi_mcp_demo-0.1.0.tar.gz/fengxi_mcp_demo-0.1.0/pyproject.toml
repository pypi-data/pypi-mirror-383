[project]
name = "fengxi-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "601125648@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.16.0",
]

[project.scripts]
fengxi-mcp-demo = "fengxi_mcp_demo:main"

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
