[project]
name = "yta-general-utils"
version = "0.2.0"
description = "Youtube Autónomo general utils are here."
authors = [
    {name = "danialcala94",email = "danielalcalavalera@gmail.com"}
]
readme = "README.md"
requires-python = "==3.9"
dependencies = [
    "yta_validation (>=0.0.1,<1.0.0)",
    "yta_constants (>=0.0.1,<1.0.0)",
    "yta_random (>=0.0.1,<1.0.0)",
    "numpy (>=1.22.0,<3.0.0)", # Previously 1.26.4
    "requests (>=2.32.3,<9.0.0)",
]

[tool.poetry]
packages = [{include = "yta_general_utils", from = "src"}]

# Optional dependencies below
[tool.poetry.group.optional]
optional = true

[tool.poetry.group.optional.dependencies]
matplotlib = ">=0.0.1,<9.0.0"
# Optional dependencies above

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
