[project]
name = "jack-mcp-server"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fu123", email = "fuqw92@126.com" }
]
requires-python = ">=3.13"
dependencies = [
    "map[cli]>=1.3.0",
]

[project.scripts]
jack-mcp-server = "jack_mcp_server:main"

[build-system]
requires = ["uv_build>=0.9.2,<0.10.0"]
build-backend = "uv_build"
