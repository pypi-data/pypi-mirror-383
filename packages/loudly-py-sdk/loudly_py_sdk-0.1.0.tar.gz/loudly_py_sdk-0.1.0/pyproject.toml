[project]
name = "loudly-py-sdk"
version = "0.1.0"
description = "Python SDK for Loudly Music API"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.25.0",
    "python-dotenv>=1.0.0"
]
readme = "README.md"
license = { text = "MIT" }

[tool.uv]
python = ">=3.8"
