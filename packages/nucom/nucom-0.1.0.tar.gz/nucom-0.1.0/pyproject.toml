[build-system]
requires = ["setuptools>=65", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "nucom"
version = "0.1.0"
description = "Numerical Computing Framework"
readme = "README.md"
license = {text = "MIT"}
authors = [{ name = "Suresh Neethimohan", email = "mohansuresh333@gmail.com" }]
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
license-files = []